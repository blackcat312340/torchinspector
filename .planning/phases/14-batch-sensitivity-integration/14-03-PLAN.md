---
phase: 14-batch-sensitivity-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - tests/test_compile.py
  - tests/test_integration.py
  - tests/test_monitor.py
autonomous: true
requirements:
  - INT-03
  - INT-04
must_haves:
  truths:
    - "Inspector with BatchSensitivityCollector works with torch.compile-wrapped models (best-effort, skip on failure)"
    - "Full training loop with all 4 collectors (convergence, WGR, LR, BSZ) produces TensorBoard scalars"
    - "Performance overhead of all collectors combined stays under 5% of total step time"
    - "Health report includes BSZ summary when check_bsz data is present"
    - "All new batch_sensitivity scalars appear in TensorBoard event files"
  artifacts:
    - path: "tests/test_compile.py"
      provides: "torch.compile compatibility test for BatchSensitivityCollector"
      contains: "test_compile_batch_sensitivity"
    - path: "tests/test_integration.py"
      provides: "Performance overhead test and full E2E integration test with BSZ"
      min_lines: 50
  key_links:
    - from: "tests/test_integration.py"
      to: "src/torchinspector/inspector.py"
      via: "Integration test exercises full Inspector with all collectors"
      pattern: "Inspector\\("
    - from: "tests/test_compile.py"
      to: "src/torchinspector/collectors/batch_sensitivity.py"
      via: "Compile test verifies BSZ collector works under torch.compile"
      pattern: "batch_sensitivity"
---

<objective>
Add integration tests for torch.compile compatibility, performance overhead verification, and full end-to-end training with all 4 metric collectors.

Purpose: Satisfies INT-03 (performance overhead <5% verified in tests) and INT-04 (torch.compile compatibility tested and documented). These are the final integration verification tests for v1.3.
Output: Extended test_compile.py and test_integration.py with BSZ-specific integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-batch-sensitivity-integration/14-CONTEXT.md
@.planning/phases/14-batch-sensitivity-integration/14-RESEARCH.md
@.planning/phases/14-batch-sensitivity-integration/14-01-SUMMARY.md
@.planning/phases/14-batch-sensitivity-integration/14-02-SUMMARY.md
@tests/test_compile.py
@tests/test_integration.py
@src/torchinspector/inspector.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/torchinspector/inspector.py (after Plan 02 wiring):
```python
class Inspector:
    def __init__(
        self, model, optimizer, log_dir, *,
        log_interval=100, micro_batch_variance=False, analysis_interval=5000, ...
    ) -> None: ...
    def step(self, **metrics: float) -> None: ...
    def close(self) -> None: ...
    # All 4 collectors wired:
    # self._weight_grad_ratio_collector (WGR)
    # self._lr_collector (LR)
    # self._batch_sensitivity_collector (BSZ)
    # self._monitor (convergence via check_convergence)
```

From tests/test_compile.py (existing pattern):
```python
has_compile = hasattr(torch, "compile")
def _try_compile(model): ...
def _try_forward(model, *args): ...
@pytest.mark.skipif(not has_compile, reason="torch.compile not available")
def test_compile_full_step(...): ...
```

From tests/test_integration.py (existing pattern):
```python
def _get_scalar_tags(log_dir: Path) -> set[str]:
    ea = EventAccumulator(str(log_dir))
    ea.Reload()
    return set(ea.Tags().get("scalars", []))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add torch.compile compatibility test for BatchSensitivityCollector</name>
  <files>tests/test_compile.py</files>
  <read_first>
    - tests/test_compile.py (full file — existing compile test patterns, skip guards, _try_compile/_try_forward helpers)
    - src/torchinspector/inspector.py (Inspector API with micro_batch_variance parameter)
    - .planning/phases/14-batch-sensitivity-integration/14-RESEARCH.md (Pattern 3: torch.compile skip guards, Pitfall 5)
  </read_first>
  <action>
    Add test to tests/test_compile.py inside TestCompileCompatibility class:

    test_compile_batch_sensitivity_no_crash:
    - @pytest.mark.skipif(not has_compile, reason="torch.compile not available")
    - Create simple Sequential model (fc1 -> relu -> fc2)
    - Compile with _try_compile(model)
    - Create Inspector(compiled, optimizer, log_dir, log_interval=1, micro_batch_variance=False)
    - ins.watch([".*fc1"])
    - Run forward + backward + step via _try_compile pattern
    - Verify ins._step == 1 (no crash)
    - ins.close()
    - If any step fails: pytest.skip("Compiled model step with BSZ collector failed")
    - This is best-effort per INT-04 and D-12: hooks may not fire under compile
  </action>
  <verify>
    <automated>pytest tests/test_compile.py::TestCompileCompatibility::test_compile_batch_sensitivity_no_crash -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_compile.py contains test_compile_batch_sensitivity_no_crash method
    - Test uses @pytest.mark.skipif(not has_compile, ...) decorator
    - Test creates Inspector with micro_batch_variance=False (basic GNS only, no batch data needed)
    - Test exercises forward + backward + step cycle with compiled model
    - Test skips gracefully if torch.compile fails (consistent with existing pattern)
    - pytest tests/test_compile.py -x -v exits 0 (tests pass or skip)
  </acceptance_criteria>
  <done>
    torch.compile compatibility test for BatchSensitivityCollector added. Follows best-effort skip pattern per INT-04.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add performance overhead test and full E2E integration test</name>
  <files>tests/test_integration.py</files>
  <read_first>
    - tests/test_integration.py (full file — existing integration test patterns, _get_scalar_tags, SimpleMLPForSmart, TestWatchAutoE2E)
    - src/torchinspector/inspector.py (Inspector API, all collector wiring)
    - .planning/phases/14-batch-sensitivity-integration/14-RESEARCH.md (Performance measurement pattern, test framework section)
    - .planning/phases/14-batch-sensitivity-integration/14-CONTEXT.md (D-08: performance overhead verification, D-11: integration test verifies performance)
  </read_first>
  <action>
    Add two test classes to tests/test_integration.py:

    1. TestBSZIntegration (full E2E with all 4 collectors):
       - test_full_training_with_all_collectors:
         - Create SimpleMLPForSmart model, Adam optimizer
         - Create Inspector with log_interval=5, health_report_interval=10, micro_batch_variance=False
         - ins.watch_auto(max_layers=3)
         - Run 30 training steps (forward, backward, step, ins.step(loss=loss.item()))
         - ins.close()
         - Verify TensorBoard event file contains batch_sensitivity/gns scalar tag
         - Verify convergence/score tag exists (convergence collector active)
         - Use _get_scalar_tags() helper

       - test_bsz_scalars_in_event_file:
         - Run 30+ steps with log_interval=5
         - After close(), verify "batch_sensitivity/gns" in scalar tags
         - Verify at least 1 GNS data point written (need >= 10 data points in window first)
         - Use _get_scalar_values() helper to check step/value pairs

    2. TestPerformanceOverhead (INT-03 verification):
       - test_collector_overhead_under_5_percent:
         - Create SimpleMLPForSmart model, Adam optimizer
         - Run baseline: 50 steps WITHOUT Inspector (just model training), measure total time via time.perf_counter()
         - Run instrumented: 50 steps WITH Inspector (log_interval=1, health_report_interval=100), measure total time
         - overhead_pct = ((instrumented - baseline) / baseline) * 100
         - Assert overhead_pct < 5.0 (per INT-03, D-08)
         - Note: This is a heuristic test; on fast machines the overhead may be unmeasurable. Use a generous tolerance and skip if baseline < 0.1s (too fast to measure reliably).
         - IMPORTANT: Use time.perf_counter() for measurement (per D-08). Run each trial multiple times (3 runs) and take the median to reduce noise.
  </action>
  <verify>
    <automated>pytest tests/test_integration.py::TestBSZIntegration tests/test_integration.py::TestPerformanceOverhead -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_integration.py contains TestBSZIntegration class with full E2E test
    - tests/test_integration.py contains TestPerformanceOverhead class with overhead measurement
    - test_full_training_with_all_collectors verifies batch_sensitivity/gns in TensorBoard scalars
    - test_bsz_scalars_in_event_file verifies GNS data points written to event file
    - test_collector_overhead_under_5_percent measures overhead and asserts < 5%
    - Performance test uses time.perf_counter() and median of 3 runs
    - pytest tests/test_integration.py::TestBSZIntegration tests/test_integration.py::TestPerformanceOverhead -x exits 0
  </acceptance_criteria>
  <done>
    Integration tests verify: all 4 collectors produce TensorBoard scalars in E2E training, BSZ-specific scalars present, performance overhead under 5% budget. INT-03 and INT-04 requirements verified.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_integration.py::TestBSZIntegration -x` passes
- `pytest tests/test_integration.py::TestPerformanceOverhead -x` passes
- `pytest tests/test_compile.py -x -v` passes or skips gracefully
- `pytest tests/ -x` full suite passes (no regressions)
</verification>

<success_criteria>
- torch.compile compatibility test exists and passes/skips per INT-04
- Performance overhead test verifies < 5% budget per INT-03
- Full E2E test verifies all 4 collectors produce TensorBoard scalars
- Full test suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/14-batch-sensitivity-integration/14-03-SUMMARY.md` when done
</output>
