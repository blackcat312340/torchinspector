---
phase: 15-utils-trendmonitor-extensions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/torchinspector/utils.py
  - tests/test_utils.py
autonomous: true
requirements:
  - INT-08
must_haves:
  truths:
    - "list_transformer_layers() returns all MHA module names and references from a model"
    - "is_transformer_model() returns True when model contains any nn.MultiheadAttention"
    - "FlashAttention models can collect attention weights via sdpa_kernel(SDPBackend.MATH) context manager"
    - "force_math_backend parameter allows users to disable SDPA backend forcing"
    - "classify_architecture() returns 'transformer' type for MHA-containing models"
  artifacts:
    - path: "src/torchinspector/utils.py"
      provides: "list_transformer_layers(), is_transformer_model(), force_math_sdpa()"
      contains: "def list_transformer_layers"
    - path: "tests/test_utils.py"
      provides: "Unit tests for transformer utils and SDPA compat"
      contains: "TestListTransformerLayers"
  key_links:
    - from: "src/torchinspector/utils.py"
      to: "list_transformer_layers()"
      via: "isinstance(module, nn.MultiheadAttention) check"
      pattern: "isinstance.*nn.MultiheadAttention"
    - from: "src/torchinspector/utils.py"
      to: "force_math_sdpa()"
      via: "torch.nn.attention.sdpa_kernel(SDPBackend.MATH)"
      pattern: "sdpa_kernel.*SDPBackend.MATH"
    - from: "src/torchinspector/utils.py"
      to: "classify_architecture()"
      via: "MHA detection returns 'transformer' type"
      pattern: "transformer"
---

<objective>
Add transformer utility functions and FlashAttention compatibility to utils.py, and extend classify_architecture() to detect transformer models.

Purpose: Phase 16-17 collectors need list_transformer_layers() to enumerate MHA modules, is_transformer_model() for auto-detection, and force_math_sdpa() for FlashAttention-safe collection. classify_architecture() must recognize transformer models for automatic feature enablement.

Output: Extended utils.py with 3 new functions and updated classify_architecture(), fully tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-utils-trendmonitor-extensions/15-CONTEXT.md
@.planning/phases/15-utils-trendmonitor-extensions/15-RESEARCH.md
@src/torchinspector/utils.py
@tests/test_utils.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add list_transformer_layers(), is_transformer_model(), and force_math_sdpa() to utils.py</name>
  <files>src/torchinspector/utils.py, tests/test_utils.py</files>
  <read_first>
    - src/torchinspector/utils.py (full file — list_mha_layers() at lines 119-135 and is_hf_model() at lines 138-150 are the templates)
    - tests/test_utils.py (full file — existing test patterns for utility functions)
  </read_first>
  <behavior>
    - list_transformer_layers(model) returns list[tuple[str, nn.MultiheadAttention]] sorted by name
    - list_transformer_layers skips root module (name == "")
    - list_transformer_layers uses isinstance(module, nn.MultiheadAttention) per D-01/D-02
    - is_transformer_model(model) returns True if any nn.MultiheadAttention module exists per D-02
    - is_transformer_model iterates model.named_modules() and checks isinstance
    - force_math_sdpa() is a context manager that wraps torch.nn.attention.sdpa_kernel(SDPBackend.MATH) per D-05
    - force_math_sdpa() has a enabled parameter (default True) per D-06 — when False, it's a no-op
    - force_math_sdpa() guards import with try/except for older PyTorch versions
    - All functions placed in utils.py alongside existing utility functions
  </behavior>
  <action>
    Add three new functions to src/torchinspector/utils.py, following existing patterns from list_mha_layers() and is_hf_model().

    1. list_transformer_layers(model: nn.Module) -> list[tuple[str, nn.MultiheadAttention]]
    - Per D-03: returns (name, module) pairs for all MHA layers
    - Iterate model.named_modules(), skip name == ""
    - isinstance(module, nn.MultiheadAttention) check per D-02
    - Return sorted by name (same pattern as list_mha_layers)
    - Note: returns tuples with module references, not just names (D-03)

    2. is_transformer_model(model: nn.Module) -> bool
    - Per D-11/D-12: returns True if model contains any MHA modules
    - Iterate model.named_modules(), check isinstance(module, nn.MultiheadAttention)
    - Return True on first match, False if none found
    - Follows is_hf_model() pattern (simple boolean check)

    3. force_math_sdpa(enabled: bool = True) -> contextlib.AbstractContextManager
    - Per D-04/D-05/D-06: context manager that forces math SDPA backend during attention weight collection
    - Import torch.nn.attention.sdpa_kernel and SDPBackend inside the function (lazy import)
    - Guard with try/except ImportError for PyTorch < 2.0 compatibility
    - When enabled=True: return sdpa_kernel(SDPBackend.MATH) context manager
    - When enabled=False: return contextlib.nullcontext() (no-op)
    - Add import contextlib at top of file

    Write tests FIRST (TDD RED):
    - TestListTransformerLayers class: test returns sorted (name, module) tuples, test empty model returns [], test skips root module, test detects nested MHA
    - TestIsTransformerModel class: test returns True for model with MHA, test returns False for model without MHA, test empty model returns False
    - TestForceMathSDPA class: test context manager can be entered/exited, test enabled=False is no-op, test SDPBackend.MATH is forced when enabled=True
    - Run tests -> confirm RED
    - Implement functions -> confirm GREEN
  </action>
  <verify>
    <automated>cd "D:/pythonprojects/DL-TOOL" && python -m pytest tests/test_utils.py::TestListTransformerLayers tests/test_utils.py::TestIsTransformerModel tests/test_utils.py::TestForceMathSDPA -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - list_transformer_layers(model) returns list of (name, module) tuples sorted by name
    - list_transformer_layers(empty_model) returns []
    - is_transformer_model(model_with_mha) returns True
    - is_transformer_model(model_without_mha) returns False
    - force_math_sdpa() can be used as a context manager without error
    - force_math_sdpa(enabled=False) is a no-op context manager
    - All new tests pass: pytest tests/test_utils.py::TestListTransformerLayers tests/test_utils.py::TestIsTransformerModel tests/test_utils.py::TestForceMathSDPA -x
  </acceptance_criteria>
  <done>list_transformer_layers(), is_transformer_model(), and force_math_sdpa() implemented in utils.py with full test coverage. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend classify_architecture() to detect transformer models</name>
  <files>src/torchinspector/utils.py, tests/test_utils.py</files>
  <read_first>
    - src/torchinspector/utils.py (classify_architecture() at lines 219-304 — understand existing detection logic)
    - tests/test_utils.py (existing classify_architecture tests)
  </read_first>
  <behavior>
    - classify_architecture() already labels MHA modules as "transformer_block" (line 272-276)
    - New: add a helper function get_architecture_type(model) -> str that returns top-level type
    - get_architecture_type checks classify_architecture() result for any "transformer_block" entries
    - If any "transformer_block" found, return "transformer"
    - If any "conv_block" found, return "cnn"
    - If any "rnn_block" found, return "rnn"
    - Otherwise return "unknown"
    - This avoids modifying classify_architecture() return type (which would break callers)
  </behavior>
  <action>
    Add get_architecture_type() function to utils.py per D-11, following the recommendation in RESEARCH.md to use a separate function rather than modifying classify_architecture() return type.

    get_architecture_type(model: nn.Module) -> str:
    - Call classify_architecture(model) to get the block type mapping
    - Check if any value[0] == "transformer_block" -> return "transformer"
    - Check if any value[0] == "conv_block" -> return "cnn"
    - Check if any value[0] == "rnn_block" -> return "rnn"
    - Otherwise return "unknown"
    - Priority: transformer > cnn > rnn > unknown (transformer wins if mixed)

    This function provides the D-12 capability: Inspector can call get_architecture_type(model) to auto-enable transformer analysis when "transformer" is returned.

    Write tests FIRST (TDD RED):
    - TestGetArchitectureType class: test returns "transformer" for MHA model, test returns "cnn" for conv model, test returns "rnn" for LSTM model, test returns "unknown" for simple model, test "transformer" wins in mixed model
    - Run tests -> confirm RED
    - Implement function -> confirm GREEN
  </action>
  <verify>
    <automated>cd "D:/pythonprojects/DL-TOOL" && python -m pytest tests/test_utils.py::TestGetArchitectureType -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - get_architecture_type(transformer_model) returns "transformer"
    - get_architecture_type(cnn_model) returns "cnn"
    - get_architecture_type(rnn_model) returns "rnn"
    - get_architecture_type(simple_model) returns "unknown"
    - classify_architecture() return type unchanged (no breaking changes)
    - All new tests pass: pytest tests/test_utils.py::TestGetArchitectureType -x
    - All existing tests still pass: pytest tests/test_utils.py -x
  </acceptance_criteria>
  <done>get_architecture_type() implemented, returning top-level architecture string. classify_architecture() unchanged. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyTorch SDPA API -> TorchInspector | sdpa_kernel/SDPBackend imports may fail on older PyTorch |
| Model input -> utils functions | Model must be nn.Module, no validation needed (PyTorch convention) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | SDPA backend forcing | accept | Only affects attention weight collection scope, not training |
| T-15-04 | Information Disclosure | force_math_sdpa no-op on older PyTorch | mitigate | try/except ImportError with clear warning message |
</threat_model>

<verification>
After both tasks complete, run the full test suite:
- `pytest tests/test_utils.py -x` — all existing + new tests pass
- `pytest tests/ -x` — no regressions across the project

Verify key behaviors:
- list_transformer_layers() returns (name, module) tuples for MHA modules
- is_transformer_model() correctly detects transformer models
- force_math_sdpa() context manager works with enabled=True/False
- get_architecture_type() returns "transformer" for MHA models
</verification>

<success_criteria>
- list_transformer_layers() exists with correct signature and returns sorted (name, nn.MultiheadAttention) tuples
- is_transformer_model() exists with correct signature and returns bool
- force_math_sdpa() exists as context manager with enabled parameter
- get_architecture_type() exists, returns "transformer"/"cnn"/"rnn"/"unknown"
- All new tests pass (TestListTransformerLayers, TestIsTransformerModel, TestForceMathSDPA, TestGetArchitectureType)
- No regressions in existing tests
</success_criteria>

<output>
Create `.planning/phases/15-utils-trendmonitor-extensions/15-02-SUMMARY.md` when done
</output>
