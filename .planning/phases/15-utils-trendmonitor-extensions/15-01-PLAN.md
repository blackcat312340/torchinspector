---
phase: 15-utils-trendmonitor-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/torchinspector/monitor.py
  - tests/test_monitor.py
autonomous: true
requirements:
  - ATTN-04
  - INT-06
must_haves:
  truths:
    - "TrendMonitor can detect attention entropy collapsing (negative slope across short+long windows)"
    - "TrendMonitor can detect QKV condition number rising (positive slope across short+long windows)"
    - "Correlation rules fire when attention collapse coincides with slow convergence"
    - "Correlation rules fire when QKV condition anomaly coincides with gradient anomaly"
    - "Alert escalation follows same thresholds as check_wgr: 5->INFO, 10->WARN, 20+acceleration->CRITICAL"
  artifacts:
    - path: "src/torchinspector/monitor.py"
      provides: "check_attention(), check_qkv(), 2 new correlation rules"
      contains: "def check_attention"
    - path: "tests/test_monitor.py"
      provides: "Unit tests for check_attention, check_qkv, correlation rules"
      contains: "TestCheckAttention"
  key_links:
    - from: "src/torchinspector/monitor.py"
      to: "check_attention()"
      via: "multi-scale window feeding (short/medium/long)"
      pattern: "attention/.*/entropy:(short|medium|long)"
    - from: "src/torchinspector/monitor.py"
      to: "check_qkv()"
      via: "multi-scale window feeding (short/medium/long)"
      pattern: "qkv/.*/cond:(short|medium|long)"
    - from: "src/torchinspector/monitor.py"
      to: "correlation_check()"
      via: "attention_collapse + convergence_slow rule"
      pattern: "attention_collapse_convergence_slow"
---

<objective>
Add check_attention() and check_qkv() methods to TrendMonitor with multi-scale trend detection, plus 2 new correlation rules for cross-metric alerting.

Purpose: TrendMonitor needs attention-aware and QKV-aware check methods before Phase 16-17 collectors can integrate. The correlation rules enable cross-metric warnings (attention collapse + slow convergence, QKV anomaly + gradient anomaly).

Output: Extended TrendMonitor with 2 new check methods and 2 new correlation rules, fully tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-utils-trendmonitor-extensions/15-CONTEXT.md
@.planning/phases/15-utils-trendmonitor-extensions/15-RESEARCH.md
@src/torchinspector/monitor.py
@tests/test_monitor.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add check_attention() and check_qkv() methods to TrendMonitor</name>
  <files>src/torchinspector/monitor.py, tests/test_monitor.py</files>
  <read_first>
    - src/torchinspector/monitor.py (full file — existing check_wgr() at lines 307-396, check_bsz() at lines 398-482 are the exact template)
    - tests/test_monitor.py (full file — existing TestCheckWGR, TestCheckBSZ test classes are the template)
  </read_first>
  <behavior>
    - check_attention(name, entropy, step) feeds 3 sub-windows: attention/{name}/entropy:short/:medium/:long
    - check_attention maintains unsuffixed window attention/{name}/entropy for correlation_check lookups
    - check_attention detects entropy falling (both slopes negative) as attention collapse
    - check_attention escalates: count>=5->INFO, >=10->WARN, >=20+acceleration->CRITICAL
    - check_attention resets alert count when no sustained trend and count < 5
    - check_qkv(name, condition_number, step) feeds 3 sub-windows: qkv/{name}/cond:short/:medium/:long
    - check_qkv maintains unsuffixed window qkv/{name}/cond for correlation_check lookups
    - check_qkv detects condition number rising (both slopes positive) as ill-conditioned
    - check_qkv uses same escalation thresholds as check_attention
    - Both methods return AlertLevel and update self._current_alerts
    - Window sizes: short=10 (_SHORT_WINDOW), medium=50 (_MEDIUM_WINDOW), long=200 (_LONG_WINDOW)
    - Trend detection for check_attention: entropy decreasing = collapse (both slopes negative -> increment count)
    - Trend detection for check_qkv: condition number rising = ill-conditioned (both slopes positive -> increment count)
    - Mixed signals or flat data -> decay count by 1 (same as check_wgr)
  </behavior>
  <action>
    Implement check_attention() and check_qkv() on TrendMonitor class, following the exact pattern of check_wgr() (lines 307-396) and check_bsz() (lines 398-482).

    For check_attention(self, name: str, entropy: float, step: int) -> AlertLevel:
    - Per D-07: single method handles entropy trend detection
    - Window keys: f"attention/{name}/entropy:short", f"attention/{name}/entropy:medium", f"attention/{name}/entropy:long"
    - Base window key: f"attention/{name}/entropy" (unsuffixed, for correlation_check)
    - Alert key: f"attention/{name}"
    - Trend logic: both short_slope < 0 and long_slope < 0 -> entropy collapsing -> increment _alert_counts[alert_key]
    - Both slopes positive -> entropy recovering -> decrement count
    - Mixed -> decrement count
    - Escalation: count >= 20 + acceleration (|short_slope| > |long_slope| * 1.5) -> CRITICAL, count >= 10 -> WARN, count >= 5 -> INFO, else OK
    - Reset: if not both falling and not both rising and count < 5 -> reset to 0, level = OK

    For check_qkv(self, name: str, condition_number: float, step: int) -> AlertLevel:
    - Per D-08: single method handles QKV condition number anomaly detection
    - Window keys: f"qkv/{name}/cond:short", f"qkv/{name}/cond:medium", f"qkv/{name}/cond:long"
    - Base window key: f"qkv/{name}/cond" (unsuffixed, for correlation_check)
    - Alert key: f"qkv/{name}"
    - Trend logic: both short_slope > 0 and long_slope > 0 -> condition number rising -> increment count
    - Both slopes negative -> condition number improving -> decrement count
    - Mixed -> decrement count
    - Same escalation thresholds as check_attention
    - Same reset logic

    Write tests FIRST (TDD RED):
    - TestCheckAttention class: test feeds 3 sub-windows, test entropy collapsing raises alert, test entropy recovering resets, test escalation thresholds (5->INFO, 10->WARN, 20+accel->CRITICAL), test mixed signals decay count
    - TestCheckQKV class: test feeds 3 sub-windows, test condition number rising raises alert, test improving resets, test escalation thresholds, test mixed signals decay count
    - Run tests -> confirm RED (methods don't exist yet)
    - Implement methods -> confirm GREEN
  </action>
  <verify>
    <automated>cd "D:/pythonprojects/DL-TOOL" && python -m pytest tests/test_monitor.py::TestCheckAttention tests/test_monitor.py::TestCheckQKV -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - check_attention("layer_0", 1.5, 1) returns AlertLevel.OK (first call, insufficient data)
    - Feeding 25+ decreasing entropy values causes alert level to escalate through INFO->WARN
    - check_qkv("layer_0", 100.0, 1) returns AlertLevel.OK (first call)
    - Feeding 25+ increasing condition numbers causes alert level to escalate
    - Window keys include :short, :medium, :long suffixes
    - Unsuffixed base window is maintained for correlation_check lookups
    - All new tests pass: pytest tests/test_monitor.py::TestCheckAttention tests/test_monitor.py::TestCheckQKV -x
  </acceptance_criteria>
  <done>check_attention() and check_qkv() methods implemented on TrendMonitor with multi-scale trend detection and alert escalation. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 2 new correlation rules to correlation_check()</name>
  <files>src/torchinspector/monitor.py, tests/test_monitor.py</files>
  <read_first>
    - src/torchinspector/monitor.py (correlation_check() method at lines 125-305 — see existing rule patterns)
    - tests/test_monitor.py (existing correlation rule tests for pattern reference)
  </read_first>
  <behavior>
    - Rule 1: attention_collapse + convergence_slow -> WARN
      - When any attention entropy window has negative slope AND _last_convergence_score < 40
      - Rule name: "attention_collapse_convergence_slow"
      - Level: AlertLevel.WARN
      - Message: "Attention entropy collapsing + slow convergence — possible attention degradation"
    - Rule 2: qkv_condition_high + gradient_anomaly -> WARN
      - When any qkv condition window latest value > 1000 AND any gradient norm window has significant slope
      - Rule name: "qkv_condition_high_gradient_anomaly"
      - Level: AlertLevel.WARN
      - Message: "High QKV condition number + gradient anomaly — possible projection matrix instability"
    - Both rules follow existing correlation_check() pattern: scan metric keys, check window data, append to alerts list
  </behavior>
  <action>
    Add 2 new correlation rules to the existing correlation_check() method in TrendMonitor, following the exact pattern of existing rules (lines 125-305).

    Rule 1 (per D-09): attention_collapse + convergence_slow
    - Scan metrics for keys containing "attention" and "entropy" (excluding :short/:medium/:long suffixes)
    - Check if self._last_convergence_score is not None and < 40
    - For each matching entropy key, compute slope of its window
    - If slope < 0 (entropy falling = collapse), append alert tuple
    - Rule name: "attention_collapse_convergence_slow", level: AlertLevel.WARN
    - Message: "Attention entropy collapsing + slow convergence — possible attention degradation"
    - Break after first match (one alert per rule)

    Rule 2 (per D-09): qkv_condition_high + gradient_anomaly
    - Scan metrics for keys containing "qkv" and "cond" (excluding :short/:medium/:long suffixes)
    - Scan metrics for keys containing "gradient" and "norm"
    - For each qkv key, check if latest value in window > 1000
    - If high, check gradient norm slopes for significant slope (abs(slope) > 0.001)
    - If found, append alert tuple
    - Rule name: "qkv_condition_high_gradient_anomaly", level: AlertLevel.WARN
    - Message: "High QKV condition number + gradient anomaly — possible projection matrix instability"
    - Break after first match

    Write tests FIRST (TDD RED):
    - TestAttentionCorrelationRules class: test attention_collapse + convergence_slow fires, test qkv_condition_high + gradient_anomaly fires, test no false positives when conditions not met
    - Run tests -> confirm RED
    - Implement rules -> confirm GREEN
  </action>
  <verify>
    <automated>cd "D:/pythonprojects/DL-TOOL" && python -m pytest tests/test_monitor.py::TestAttentionCorrelationRules -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - correlation_check() returns "attention_collapse_convergence_slow" rule when attention entropy windows have negative slope and convergence_score < 40
    - correlation_check() returns "qkv_condition_high_gradient_anomaly" rule when qkv cond window latest > 1000 and gradient norm has significant slope
    - No false positives: rules do not fire when preconditions are not met
    - All existing correlation rule tests still pass (no regressions)
    - New tests pass: pytest tests/test_monitor.py::TestAttentionCorrelationRules -x
  </acceptance_criteria>
  <done>Two new correlation rules added to correlation_check(). Rules fire correctly when attention collapse coincides with slow convergence, and when QKV condition anomaly coincides with gradient anomaly. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Metric input -> TrendMonitor | Float values from collectors enter check_attention/check_qkv — must handle NaN/Inf gracefully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | TrendMonitor window data | accept | Internal-only metric data, no external input surface |
| T-15-02 | Denial of Service | Window memory growth | mitigate | All windows capped at _LONG_WINDOW (200) or _window_size via pop(0) |
</threat_model>

<verification>
After both tasks complete, run the full test suite:
- `pytest tests/test_monitor.py -x` — all existing + new tests pass
- `pytest tests/ -x` — no regressions across the project

Verify key behaviors:
- check_attention() returns AlertLevel for decreasing entropy
- check_qkv() returns AlertLevel for rising condition number
- correlation_check() includes attention_collapse and qkv_condition rules
- Window key names follow namespace convention: attention/{name}/entropy, qkv/{name}/cond
</verification>

<success_criteria>
- check_attention() method exists on TrendMonitor with correct signature (name, entropy, step) -> AlertLevel
- check_qkv() method exists on TrendMonitor with correct signature (name, condition_number, step) -> AlertLevel
- 2 new correlation rules in correlation_check(): attention_collapse_convergence_slow, qkv_condition_high_gradient_anomaly
- All new tests pass (TestCheckAttention, TestCheckQKV, TestAttentionCorrelationRules)
- No regressions in existing tests
</success_criteria>

<output>
Create `.planning/phases/15-utils-trendmonitor-extensions/15-01-SUMMARY.md` when done
</output>
