---
phase: 13-lr-scheduler-analysis
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/torchinspector/inspector.py
  - src/torchinspector/collectors/__init__.py
  - src/torchinspector/monitor.py
  - tests/test_collectors/test_lr_scheduler.py
autonomous: true
requirements:
  - LR-03
  - INT-02
must_haves:
  truths:
    - "Inspector initializes LRCollector with optimizer, backend, monitor, log_interval, warmup_steps"
    - "Inspector.step() calls LRCollector.collect() at log_interval with loss_val"
    - "Inspector.close() calls LRCollector.close()"
    - "TrendMonitor.correlation_check() includes lr_spike + loss_stagnant rule"
    - "lr_warmup_steps parameter is configurable on Inspector.__init__"
  artifacts:
    - path: "src/torchinspector/inspector.py"
      provides: "LRCollector wiring in Inspector"
      contains: "_lr_collector"
    - path: "src/torchinspector/collectors/__init__.py"
      provides: "LRCollector export"
      contains: "LRCollector"
    - path: "src/torchinspector/monitor.py"
      provides: "lr_spike + loss_stagnant correlation rule"
      contains: "lr_spike_loss_stagnant"
  key_links:
    - from: "src/torchinspector/inspector.py"
      to: "src/torchinspector/collectors/lr_scheduler.py"
      via: "Inspector imports and instantiates LRCollector"
      pattern: "from torchinspector.collectors.lr_scheduler import LRCollector"
    - from: "src/torchinspector/inspector.py"
      to: "LRCollector.collect()"
      via: "Inspector.step() calls _lr_collector.collect() at log_interval"
      pattern: "_lr_collector\\.collect\\("
    - from: "src/torchinspector/monitor.py"
      to: "correlation_check()"
      via: "lr_spike + loss_stagnant rule in correlation_check"
      pattern: "lr_spike_loss_stagnant"
---

<objective>
Wire LRCollector into Inspector and add lr-spike + loss-stagnation correlation rule to TrendMonitor.

Purpose: Completes INT-02 partial (lr-spike + loss-stagnation -> WARN correlation rule) and LR-03 (lr-loss correlation visibility). Inspector gains lr_warmup_steps parameter and LRCollector lifecycle management.
Output: Modified inspector.py, __init__.py, monitor.py correlation rule, extended integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-lr-scheduler-analysis/13-CONTEXT.md
@.planning/phases/13-lr-scheduler-analysis/13-RESEARCH.md
@.planning/phases/13-lr-scheduler-analysis/13-01-SUMMARY.md
@src/torchinspector/inspector.py
@src/torchinspector/collectors/__init__.py
@src/torchinspector/monitor.py
@tests/test_collectors/test_lr_scheduler.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/torchinspector/collectors/lr_scheduler.py (created in Plan 01):
```python
class LRCollector:
    def __init__(
        self,
        optimizer: torch.optim.Optimizer,
        backend: TensorBoardBackend,
        monitor: TrendMonitor,
        log_interval: int = 100,
        warmup_steps: int = 100,
    ) -> None: ...
    def collect(self, step: int, loss_val: float | None = None) -> None: ...
    def close(self) -> None: ...
```

From src/torchinspector/monitor.py (Plan 01 added):
```python
class TrendMonitor:
    def check_lr(self, lr: float, step: int) -> AlertLevel: ...
    def check_lr_stagnation(self, step: int) -> None: ...
    def correlation_check(self, metrics: dict[str, float]) -> list[tuple[str, AlertLevel, str]]:
        """Existing method — add lr_spike + loss_stagnant rule"""
```

From src/torchinspector/inspector.py (current):
```python
class Inspector:
    def __init__(self, model, optimizer, log_dir, *, log_interval=100, ...): ...
    def step(self, **metrics: float) -> None: ...
    def close(self) -> None: ...
    # Existing collector wiring pattern:
    self._weight_grad_ratio_collector = WeightGradRatioCollector(
        model, self._hook_manager, self._backend, self._monitor, log_interval=log_interval,
    )
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire LRCollector into Inspector</name>
  <files>src/torchinspector/inspector.py, src/torchinspector/collectors/__init__.py</files>
  <read_first>
    - src/torchinspector/inspector.py (full file — current collector wiring pattern in __init__, step(), close())
    - src/torchinspector/collectors/__init__.py (current exports list)
    - src/torchinspector/collectors/lr_scheduler.py (LRCollector API from Plan 01)
    - src/torchinspector/collectors/weight_grad_ratio.py (reference wiring pattern)
  </read_first>
  <behavior>
    - Inspector.__init__ imports and creates LRCollector with optimizer, backend, monitor, log_interval, warmup_steps
    - Inspector.__init__ accepts lr_warmup_steps: int = 100 parameter
    - Inspector.step() calls _lr_collector.collect(step, loss_val=loss_val) at log_interval
    - Inspector.close() calls _lr_collector.close()
    - collectors/__init__.py exports LRCollector
  </behavior>
  <action>
    Modify src/torchinspector/inspector.py:

    1. Add import: from torchinspector.collectors.lr_scheduler import LRCollector

    2. Add lr_warmup_steps: int = 100 parameter to Inspector.__init__() signature (after health_report_interval)

    3. In __init__, after self._weight_grad_ratio_collector = ..., add:
       self._lr_collector = LRCollector(
           optimizer,
           self._backend,
           self._monitor,
           log_interval=log_interval,
           warmup_steps=lr_warmup_steps,
       )

    4. In step(), inside the if self._step % self._log_interval == 0 block, after self._weight_grad_ratio_collector.collect(self._step), add:
       self._lr_collector.collect(self._step, loss_val=loss_val)

    Note: loss_val is already computed earlier in step() as metrics.get("loss"). If loss_val is None, LRCollector handles it gracefully.

    5. In close(), before self._weight_grad_ratio_collector.close(), add:
       self._lr_collector.close()

    Modify src/torchinspector/collectors/__init__.py:
    1. Add import: from torchinspector.collectors.lr_scheduler import LRCollector
    2. Add "LRCollector" to __all__ list
  </action>
  <verify>
    <automated>pytest tests/test_collectors/test_lr_scheduler.py tests/test_collectors/test_weight_grad_ratio.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/torchinspector/inspector.py contains from torchinspector.collectors.lr_scheduler import LRCollector
    - Inspector.__init__ has lr_warmup_steps: int = 100 parameter in signature
    - Inspector.__init__ creates self._lr_collector = LRCollector(...)
    - Inspector.step() calls self._lr_collector.collect(self._step, loss_val=loss_val) inside log_interval block
    - Inspector.close() calls self._lr_collector.close()
    - src/torchinspector/collectors/__init__.py contains "LRCollector" in __all__
    - python -c "from torchinspector import Inspector; print('OK')" exits 0
    - pytest tests/test_collectors/test_lr_scheduler.py tests/test_collectors/test_weight_grad_ratio.py -x exits 0
  </acceptance_criteria>
  <done>
    LRCollector is wired into Inspector lifecycle (init/step/close). LRCollector appears in collectors/__init__.py exports. All existing tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lr_spike + loss_stagnant correlation rule to TrendMonitor</name>
  <files>src/torchinspector/monitor.py, tests/test_collectors/test_lr_scheduler.py</files>
  <read_first>
    - src/torchinspector/monitor.py (full file — existing correlation_check() rules, especially loss_stagnant_lr_decreasing pattern)
    - tests/test_collectors/test_lr_scheduler.py (existing tests from Plan 01)
    - .planning/phases/13-lr-scheduler-analysis/13-CONTEXT.md (D-08: lr-spike + loss-stagnation -> WARN)
    - .planning/phases/13-lr-scheduler-analysis/13-RESEARCH.md (correlation rule example code)
  </read_first>
  <behavior>
    - correlation_check() includes rule: lr_spike active AND loss stagnant -> WARN (INT-02 partial)
    - Rule checks lr/anomaly window for active anomaly (value > 0 for spike)
    - Rule checks loss slope < 0.001 (flat) via _compute_slope
    - Returns ("lr_spike_loss_stagnant", AlertLevel.WARN, "LR anomaly + loss plateau — check scheduler configuration")
  </behavior>
  <action>
    Modify src/torchinspector/monitor.py — add to correlation_check() method:

    After the existing "loss_stagnant AND lr_decreasing" rule (around line 193-204), add:

    # Rule: lr_spike anomaly active AND loss stagnant → WARN (INT-02 partial, D-08)
    lr_anomaly_keys = [k for k in metrics if "lr_anomaly" in k or "lr/anomaly" in k]
    loss_keys_for_lr = [
        k for k in metrics if "loss" in k.lower()
        and not k.endswith((":short", ":medium", ":long"))
    ]
    for ak in lr_anomaly_keys:
        if metrics.get(ak, 0) > 0:  # anomaly active (spike = 1.0)
            for lk in loss_keys_for_lr:
                loss_slope = self._compute_slope(self._windows.get(lk, []))
                if loss_slope is not None and abs(loss_slope) < 0.001:
                    alerts.append((
                        "lr_spike_loss_stagnant",
                        AlertLevel.WARN,
                        "LR anomaly + loss plateau — check scheduler configuration",
                    ))
                    break
            break  # One alert per rule

    Extend tests/test_collectors/test_lr_scheduler.py with class TestCorrelationRules:
    - test_lr_spike_and_loss_stagnant_triggers_warn: feed lr/anomaly=1.0 and flat loss values into monitor windows, call correlation_check(), verify ("lr_spike_loss_stagnant", AlertLevel.WARN, ...) in result
    - test_lr_spike_and_loss_decreasing_no_alert: feed lr/anomaly=1.0 and decreasing loss, verify lr_spike_loss_stagnant NOT in result
    - test_no_lr_anomaly_no_rule: feed loss stagnation without lr/anomaly, verify lr_spike_loss_stagnant NOT in result
  </action>
  <verify>
    <automated>pytest tests/test_collectors/test_lr_scheduler.py -x -v -k "TestCorrelationRules"</automated>
  </verify>
  <acceptance_criteria>
    - TrendMonitor.correlation_check() contains "lr_spike_loss_stagnant" rule
    - Rule triggers AlertLevel.WARN when lr/anomaly > 0 and loss slope < 0.001
    - Rule message is "LR anomaly + loss plateau — check scheduler configuration"
    - test_lr_spike_and_loss_stagnant_triggers_warn passes
    - test_lr_spike_and_loss_decreasing_no_alert passes
    - test_no_lr_anomaly_no_rule passes
    - pytest tests/test_collectors/test_lr_scheduler.py -x exits 0
  </acceptance_criteria>
  <done>
    TrendMonitor.correlation_check() includes lr_spike + loss_stagnant -> WARN rule (INT-02 partial). All tests pass.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_collectors/test_lr_scheduler.py -x` passes all tests
- `pytest tests/test_monitor.py -x` passes all tests
- `pytest tests/ -x` full suite passes (no regressions)
- `python -c "from torchinspector import Inspector; print('OK')"` imports successfully
</verification>

<success_criteria>
- Inspector has lr_warmup_steps parameter and LRCollector lifecycle
- TrendMonitor.correlation_check() includes lr_spike + loss_stagnant rule
- Full test suite passes: pytest tests/ -x
- INT-02 partial requirement satisfied
</success_criteria>

<output>
Create `.planning/phases/13-lr-scheduler-analysis/13-02-SUMMARY.md` when done
</output>
